<section class="features">
  <div class="features-grid">
    <div class="feature-card">
      <h3>Prices and the data that settles them.</h3>
      <p>Market prices from Kalshi and Polymarket. The weather, economic, and source data their contracts settle on. One SDK, one&nbsp;schema.</p>
    </div>
    <div class="feature-card">
      <h3>Clean, ML-ready data.</h3>
      <p>Every reading is validated, deduplicated, and matched to the correct source before it reaches your model. Bad data, zeros, and NaNs get caught at ingest, not in your&nbsp;backtest.</p>
    </div>
    <div class="feature-card">
      <h3>Built natively for AI agents.</h3>
      <p>Responses ship in TOON format, saving 40-60% on tokens. Your agent calls <code>as_tools()</code>, discovers every method, and builds a strategy without you writing glue&nbsp;code.</p>
    </div>
    <div class="feature-card">
      <h3>Seamlessly from research to live.</h3>
      <p>Live endpoints return the same schema, units, and format as historical data. Train your model on backtests and deploy it without rewriting a single&nbsp;query.</p>
    </div>
  </div>
</section>
