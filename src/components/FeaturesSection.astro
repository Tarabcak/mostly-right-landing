<section class="features">
  <div class="features-grid">
    <div class="feature-card">
      <h3>One schema for every platform.</h3>
      <p>We normalize Kalshi and Polymarket into one consistent schema. Prices, settlements, and order book history all share the same&nbsp;structure.</p>
    </div>
    <div class="feature-card">
      <h3>Every source, already parsed.</h3>
      <p>Weather lives in GRIB files, macro hides in PDFs, and earnings sit in transcripts. We source it, clean it, and structure the data that drives each&nbsp;contract.</p>
    </div>
    <div class="feature-card">
      <h3>Backtests just work in production.</h3>
      <p>Live endpoints return the same shape as historical data. Train your model on backtests and deploy it without rewriting a single&nbsp;query.</p>
    </div>
    <div class="feature-card">
      <h3>Up to 60% fewer tokens than JSON.</h3>
      <p>Responses ship in TOON format by default, cutting token cost by 40-60%. Resolution criteria and settlement logic come included.&nbsp;</p>
    </div>
  </div>
</section>
